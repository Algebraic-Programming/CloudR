.. _cloudr:

***************
Overview
***************

CloudR is an emulator for cloud deployment that uses MPI. At startup, only one rank is available to the users. The others can be requested and relinquished via the instance manager functionalities.